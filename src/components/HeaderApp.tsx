import "../scss/Header.scss"
import React from "react";

const HeaderApp : React.FC = () => {
    return (
        <header id="header-app">
            <h1>Stocker master pro !</h1>
        </header>
    );
};

export default HeaderApp;